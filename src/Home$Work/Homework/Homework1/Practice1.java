package Home$Work.Homework.Homework1;

public class Practice1 {
    public static void main(String[] args) {
        //String fullName = sc.nextLine();
        //String stateCode = sc.nextLine();
        //String city = sc.nextLine();
       // String gender = sc.nextLine();
        //int age = sc.nextInt();
        // 17 -->
       // boolean isAbleToVote = age>15& fullName.startsWith("A") &fullName.endsWith("V") & city.length()<+10
                //& gender.equals("F") & stateCode.startsWith("I")& stateCode.endsWith("L");
       //System.out.println(isAbleToVote);
        // I can use only one & sign
        // When you use && when it understands the result
        // it will not check rest of condition
        // When you use one& sign it will check all condition

    }
}
